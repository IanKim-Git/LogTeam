<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="progUser">

	<select id="IdPwCheck" parameterType="puBean" resultType="puBean">
		select uemail, uname, upw, uphone, uprocnt from proguser where uemail=#{uemail} and upw=#{upw}
	</select>
	
	<select id="SelectAll" resultType="puBean">
		select uemail, uname, upw, uphone, uprocnt from proguser
	</select>

</mapper>
